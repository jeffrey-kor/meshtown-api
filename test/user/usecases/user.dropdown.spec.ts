// https://github.com/kberbic/ddd/blob/master/src/tests/0.user.service.test.ts
describe("User dropdown scenario test.", () => {

  it("Should be ensured if the users are themselves or not", async () => {});

  it("Should be ensured if the token for accessing is valid or not", async () => {});

  it("Should be destroyed if the token is still valid", async () => {});

  it("Should be deleted the user information in own database", async () => {});

  it("Should return true or false whether the user is deleted well or not", async () => {

  });

});