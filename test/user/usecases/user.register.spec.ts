// https://github.com/kberbic/ddd/blob/master/src/tests/0.user.service.test.ts
describe("User dropdown scenario test.", () => {

  it("Should create a user", async () => {});

  it("Should hash password before insert into database", async () => {});

  it("Should send phone message to user for welcome to meshtown", async () => {})

  it("Should return a user information to views", async () => {})

});